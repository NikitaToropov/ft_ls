#include "ft_ls.h"

t_dir *dirs_parser(char **argv, unsigned short flags)
{
	t_dir *root;

	(void)argv;
	(void)flags;
	if (*argv == NULL)
		;
	else
		;
//	get_content(root, flags);
	root = NULL;
	return (root);
}

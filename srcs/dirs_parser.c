#include "ft_ls.h"

t_dir *dirs_parser(char **argv, unsigned short flags)
{
	t_dir *head;

	(void)argv;
	(void)flags;
	if (*argv == NULL)
		;
	else
		;
//	get_content(head, flags);
	head = NULL;
	return (head);
}
